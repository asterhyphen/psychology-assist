class AppSessionStore {
  const AppSessionStore();

  Future<void> save(Object session) async {
    // Persistence is not implemented yet.
    // This placeholder keeps the app compiling and allows
    // session state to be provided through Riverpod.
  }
}
